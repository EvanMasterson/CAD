<p id="notice"><%= notice %></p>
<table class="table">
  <thead>
    <tr>
      <th>Email</th>
      <th>Admin</th>
      <th>Doctor</th>
      <th>Patient</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><%= @user.email %></td>
      <td><%= @user.admin %></td>
      <td><%= @user.doctor %></td>
      <td><%= @user.patient %></td>
    </tr>
  </tbody>
</table>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
