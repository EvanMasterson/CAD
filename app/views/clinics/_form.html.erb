<%= form_with(model: clinic, local: true) do |form| %>
  <% if clinic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(clinic.errors.count, "error") %> prohibited this clinic from being saved:</h2>

      <ul>
      <% clinic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :clinic_name %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location, id: :clinic_location %>
  </div>

  <div class="field">
    <%= form.label :specialisation %>
    <%= form.text_field :specialisation, id: :clinic_specialisation %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :clinic_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
